experiment:
  name: "bimodal_semantic_tfidf_scifact"
  output_dir: "./outputs/experiments/bimodal"

# Single dataset (scifact)
dataset:
  name: "scifact"
  root: "./data/scifact/processed/beir"
  split_preference: ["test", "dev", "validation", "train"]

# Alternative: Multiple datasets (commented)
# datasets:
#   - name: "scifact"
#     root: "./data/scifact/processed/beir"
#     split_preference: ["test", "dev", "validation", "train"]
#   - name: "fiqa"
#     root: "./data/fiqa/processed/beir"
#     split_preference: ["test", "dev", "validation", "train"]
#   - name: "nfcorpus"
#     root: "./data/nfcorpus/processed/beir"
#     split_preference: ["test", "dev", "validation", "train"]

retrievers:
  - name: "bimodal_semantic_tfidf"
    type: "hybrid"
    vectorizer:
      type: "bi_modal"
      semantic:
        model: "sentence-transformers/all-MiniLM-L6-v2"
        query_prefix: ""
        doc_prefix: ""
      tfidf:
        dim: 1000
        min_df: 1
        backend: "sklearn"
    fusion:
      strategy: "weighted_cosine"
      policy: "static"
      weights: [0.5, 0.5]  # [semantic, tfidf]
    reranker:
      type: "bi_modal"
      topk_first: 150
    index:
      type: "faiss"
      factory: null  # FlatIP
      metric: "ip"

metrics:
  - "nDCG"
  - "MRR"
  - "MAP"
  - "Recall"
  - "Precision"

ks: [1, 3, 5, 10]

output_formats:
  - "csv"
  - "json"

